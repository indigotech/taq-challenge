import { LoaderContainer } from "../styles/Loader-style"
import { Ring } from 'react-awesome-spinners'

function Preloader() {
  return(
    <LoaderContainer>
      <Ring />
    </LoaderContainer>
  )}

export default Preloader
